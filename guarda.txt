	int fd;
	size_t	len;
	char	*line;
	char	**matrix;
		
	fd = open("texto.txt", O_RDWR);
	if (fd == -1)
	{
		ft_printf("error");
		return (0);
	}
	line = get_next_line(fd);
	len = ft_strlen(line);
	while (line)
	{
		if (this_rectangular(line, len))
			return (0);
		ft_printf(line);
		free(line);
		line = get_next_line(fd);
	}
	
	close(fd);
	return (0);